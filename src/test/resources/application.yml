# 测试专用配置：上下文冒烟测试会装配 OpenAI starter，但测试不发起真实调用，
# 给一个占位 Key 即可满足自动装配。
spring:
  ai:
    openai:
      api-key: test-dummy-key
    # 对话记忆走 JDBC 实现，测试库用内嵌 H2（无需本地 MySQL）。
    chat:
      memory:
        repository:
          jdbc:
            initialize-schema: embedded
  datasource:
    url: jdbc:h2:mem:chatmem;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
